# 電圧制御の自動チューニング 引き継ぎ

最終更新: 2026-09-24　／　前提の文書: [`HANDOFF_VOLTAGE_CONTROL.md`](HANDOFF_VOLTAGE_CONTROL.md)（特に 5.8〜5.10、6章）

## 1. やりたいこと
- 電圧制御（疑似トルク制御）の足回りで、**最高の加速度・速度が出る閾値・ゲインを自動で見つける仕組み**を作る。
- これまでは「書き込み → 床で動作パターンのテスト → ST-Link で RAM のログを読む → 解析 → 手で値を変える」を1回ずつ繰り返して調整してきた（5.10）。これを自動化したい。
- ユーザーの当初の案: 動作パターンのテストに近いものを数回繰り返し、実行のたびに内部の閾値を更新して、最適な値を見つける。
- **この文書の7章に、案に対する改善点をまとめた。実装の前にユーザーと方針（8章）を決めること。**

## 2. 現状（ブランチ `FW/MainBoard_V26_2_tc`、コミット `d3ce39ec`、push済み）
- 試合の経路（Rock5A からの指令）は電圧制御で動く設定（`ROBOT_USE_VOLTAGE_CONTROL=1`）。**Rock5A の指令で車輪が回り機体が動くことは、2026-09-24 に確認した（機体を浮かせた状態。床での走行は未確認）。11章参照。**
- `main_mode.c` の Rock5A 未接続時の処理は `LocalController_Stop`（止まる）。テストを使うときだけ書き換える。
  - → 段階1（10.3）以後は、動作パターンのテストは ST-Link から開始の指示（`tools/autotune_start.ps1`）を書いたときだけ走る。書き換えは要らない。
- 処理の流れ（1msごと、`OmniDrive_SetVelEx` の電圧制御の分岐、`src/unit/omni_drive.c`）:
  1. 指令 (vx, vy, ω) → `TCS_Update` の S字加減速（加速度・角加速度の上限あり）
  2. 各輪の電圧 = 「その輪の実際の回転数で転がり続ける電圧（center）」＋「超える分（＝トルク）」
  3. 超える分 = FF（表の逆引き＋床の負荷分＋機体加速度×係数を力の配分 `force_alloc` で4輪へ）＋ 機体速度の PI（オドメトリ・ジャイロ）
  4. 超える分を ±`VOLT_TRACTION_LIMIT_V`（トルク上限）と電圧上限 4.9V に収める。先に FF の分を4輪同じ比率で縮め、足りなければ PI の分を縮める。

### 2.1 調整の対象になる値（`src/config/parammeter.h`、現在値）
| 値 | 現在 | 試合で効くか | メモ |
|---|---|---|---|
| `VOLT_TRACTION_LIMIT_V` | 2.8 V | ○ | 主役。2.0→2.4→3.2→2.8 と手で調整 |
| `VOLT_MODE_MAX_ACCEL` | 5.0 m/s² | ○ | S字の加速度上限。Rock5A の経路計画の加速度と合わせる必要がある |
| `VOLT_MODE_MAX_ANG_ACCEL` | 38 rad/s² | ○ | 上げすぎると旋回が行き過ぎる |
| `TCS_MAX_JERK` / `TCS_MAX_ANG_JERK` | 250 / 600 | ○ | 速度モードと共通。電圧制御では未調整 |
| `WHEEL_VOLT_KA_LIN_BODY` / `_ANG_BODY` | 0.5 / 0.0175 | ○ | 加減速ぶんの FF 係数 |
| `VEL_FB_KP_LIN` / `KI_LIN` | 1.5 / 5.0 | ○ | 並進の PI |
| `VEL_FB_KP_ANG` / `KI_ANG` | 0.2 / 1.5 | ○ | 回転の PI |
| `VEL_FB_I_MAX_V` | 2.0 V | ○ | 積分の上限 |
| テストの外側ループ（`local_controller.c` の `kBrakeAccel=4.5`、`kPosKp=8`、`kHeadingKp=10`、`kHeadingKd=0.5`、`TEST_PATTERN_*`） | | **×** | 試合では Rock5A が経路を作るので、**ここを最適化しても試合の動きは良くならない**（7章の2） |

- 表（`kTableOmegaPos/Neg`）と床の負荷分（`kLoadVolt`）は `src/control/wheel_voltage.c` の定数。浮かせた測定と床の TCS テストから決めた。

### 2.2 手動調整で分かったこと（5.10 の要約）
| トルク上限 | 動作パターン13区間の時間 | スリップの割合（区間により） | ユーザーの評価 |
|---|---|---|---|
| 2.0 V | 約23.1 秒 | 11〜66% | |
| 2.4 V | 約20.3 秒 | 28〜64% | 前後左右で「若干」スリップが見える |
| 3.2 V | 約18.4 秒 | 50〜87% | 斜めはかなり良い／前後左右はスリップが目立ちすぎ／旋回で行き過ぎ |
| **2.8 V（確定）** | 約19.2 秒 | 前後左右 58〜69%、斜め 29〜48% | 手動調整はここで確定 |

- 速さとスリップは、はっきり引き換え。**「最速」だけを目的にすると、ユーザーが嫌った 3.2V 側に寄る。** 目的関数にスリップや正確さを入れる必要がある（7章の3）。
- 前後左右は4輪がそろって力を出すのでトルク上限に掛かる前に滑る。斜めは2輪に力が集中し、その2輪がトルク上限に早く掛かって全体が抑えられる。**向きごとに限界が違う**（7章の6）。
- 旋回の行き過ぎは、外側ループが実際の角速度を見ていなかったのが原因で、D項を足して解消（2.0V:0〜13° → 3.2V:28〜37° → PD後 1〜6°）。自動調整でゲインを上げると、同じ種類の問題が別の場所で出うる。
- `is_slipping`（TCS のスリップ判定）は、速度モード用に決めた閾値のまま。電圧制御で強く加減速すると、本当の滑り以外でも立つ可能性がある。**そのまま目的関数に使うと、最適化が判定の癖を突く**（7章の4）。
- 1回ごとのばらつきがある（同じ設定でも旋回区間の時間が 0.9〜1.3 秒）。1回の比較で良し悪しを決めない。

## 3. 使えるデータと道具
- RAM ロガー `src/control/tcs_log.c`: 62 byte/サンプル × 1000（約 62 KB）。列は `HANDOFF_VOLTAGE_CONTROL.md` 6章。動作パターンのテストでは 30ms 周期、`tcs_on` 列＝区間の番号、`slip` の bit6＝出力を縮めた、bit7＝`is_slipping`。
- RAM の読み出し（走行後に ST-Link を挿す）:
  - アドレスはビルドごとに変わる。`arm-none-eabi-gdb -batch -ex "print &'tcs_log.c'::samples" -ex "print &'tcs_log.c'::sample_count" build/MainBoard_V26_2.elf`（gdb は `C:\ST\STM32CubeCLT_1.21.0\GNU-tools-for-STM32\bin\`）
  - `STM32_Programmer_CLI -c port=SWD mode=HOTPLUG -r16 <sample_count> 1` → `-u <samples> <n*62> file.bin`
  - デコード: t_ms u16 @0, tag u8 @2, slip u8 @3, int16 ×23 @4..48, rx_stall u8 @50, rx_restart u16 @52, 電圧 int16 ×4 @54..60（×0.01V）
  - → 上の手順は `tools/read_tcs_log.ps1` にまとめた（10.3）。
  - 過去の走行: `docs/data/motion_pattern_*.csv`、`logs/motion_*_ram.csv`（`logs/` は git 対象外）
- MainBoard → Rock5A の SPI（`docs/SPI_PROTOCOL.md`）: 1ms ごとに **4輪の実角速度と IMU（加速度 xy・ヨー角速度・姿勢 yaw）、電池電圧**を送っている。Rock5A → MainBoard は機体速度 (vx, vy, ω) とキック等。**フレーム（21 byte）に空きは無い。**
- UI シリアル（`Robot_UpdateFromUi`）はドリブル・キック・充電の指示だけ。パラメータを書き換えるコマンドは無い。
- ボタン・DIP スイッチの入力は MainBoard のコードに無い。

## 4. 制約・注意（前のチャットで実際に踏んだもの）
- **PC にホスト用の C コンパイラ・Python は無い**（`python` は Store のスタブ）。解析は PowerShell 5.1 で行ってきた。
  - スクリプトは **UTF-8 BOM 付き**で保存（BOM なしは CP932 として読まれ、日本語コメントが次の行を飲み込む）。
  - 変数名は大文字小文字を区別しない（`$h` と `$H`、`$s` と `$S` が衝突した）。
  - `@(a, b/1000.0)` はカンマの方が先に結び付く。割り算は括弧で囲む。
  - 正規表現の `{4}?` は「省略可」ではなく最短一致。
- ビルドは PowerShell で `make -j`（Git Bash は TEMP の問題で gcc が失敗する）。書き込みはユーザーが行ってきた。
- **ST-Link を挿したままだと WheelUnit からの受信が途切れやすい**（5.2、原因未調査）。走行中の ST-Link は抜いてきた。そもそも機体が 2〜3m 動くので、ケーブルをつないだまま走らせるのは難しい。
- 走行の場所: 動作パターン（1.5倍）で、スタート位置から前に約2.0m、後ろに約0.5m、左右に約2.0m。
- WheelUnit の保護: **+5.0V ちょうどが1秒続くと出力を止める**（正側のみ）、60℃超で停止（50℃で復帰）、電源15V未満で停止。自動で何度も強く走らせると、モータの温度と電池電圧が変わっていく。
- 安全: テストは `main_mode.c` の「Rock5A 未接続時」の分岐から呼んでいる。**戻し忘れると、Rock5A 未接続のときに機体が勝手に動く。** 自動調整を入れるなら、明示的な操作でだけ始まるようにする（7章の8）。

## 5. 当初の案（ユーザー）
MainBoard 上で、動作パターンのテストに近いものを数回繰り返し、1回ごとに閾値を更新して最適な値を探す。

## 6. 案の良いところ
- ケーブルも通信の拡張も要らない。今のテストと RAM ロガーをそのまま使える。
- 動作パターンは閉ループで原点に戻るので、続けて何回か走らせられる。

## 7. 改善点（提案）
1. **「測って決められる値」と「探すしかない値」を分ける。** 多くの値は物理量で、探索しなくても専用の走行で直接測れる。
   - 例: トルク上限を1回の走行の中で少しずつ上げ、向きごとに滑り始める所を見つける（=向きごとの床の限界）。実際に出せた加減速度から `VOLT_MODE_MAX_ACCEL` と外側ループの想定減速度を決める。旋回も同じ。
   - 探索に残すのは、速さと滑りの引き換えのような、ユーザーの好みで決まる少数の値だけ。次元が減るほど、少ない走行回数で決まる。
2. **試合で効く値だけを調整する。** 外側ループ（`kPosKp` など）はテスト専用で、試合では Rock5A が経路を作る。外側ループは固定し、内側（トルク上限・S字の上限・FF・PI）だけを動かす。さらに、見つけた加速度の上限を **Rock5A 側の経路計画の加速度の上限にも反映**しないと、試合では活きない。
3. **目的関数は「最速」ではなく「制約付きの最速」にする。** 例: 区間の時間を最小化。ただし、止まった位置のずれ ≤ 5cm、向きの行き過ぎ ≤ 5°、進む向きからの外れ ≤ ○cm、安全停止なし。スリップの割合はペナルティか上限にする。重みや上限は、ユーザーの評価（2.4V=許容、3.2V=滑りすぎ、2.8V=確定）に合うように決める。
4. **測り方そのものを信用しすぎない。** 位置も速度も車輪のオドメトリで、滑るほど狂う。最適化は測定の誤りを突きやすい（「着いた」とオドメトリが言っても実際は行き過ぎている、など）。
   - 最善: **SSL-Vision（天井カメラ）で実際の位置を測る。** 使えるかユーザーに確認する。
   - 次善: 数回ごとに実際の止まった位置を人が測る、IMU の積分と照合する、スリップ判定の閾値を電圧制御用に見直す。
5. **ばらつきに強くする。** 1つの設定を1回で評価しない（2〜3回、または区間をまとめて平均）。探索は少ない回数で済む方法（1〜2変数ずつの黄金分割探索や座標探索）から始める。
6. **向きごとの限界を持たせる。** 今は全方向で同じトルク上限・加速度上限。前後・左右・斜めで限界が違うことが分かっているので、向きごとの加速度上限（楕円の摩擦円など）を持たせると、斜めを削らずに前後左右の滑りを減らせる可能性がある。
7. **会場で短時間に走らせ直せる「較正手順」として作る。** 大会のフィールド（カーペット）は練習の床とグリップが違うので、今の床で決めた値はそのまま使えない。狭い場所・数分で終わる手順にしておくと価値が大きい。長期的には、固定の閾値ではなく、走行中に滑りを見てトルクを絞る本来の TCS（実行時の閉ループ）の方が床の違いに強い。
8. **安全の仕組みを先に作る。** 自動で強くしていくので:
   - 明示的な操作（UI シリアルのコマンドなど）でだけ始まる。Rock5A 未接続時の分岐に置かない。
   - 探索範囲の上限を固定（例: トルク上限 ≤ 3.2V）。安全停止（範囲外・向きのずれ・時間切れ）は必ず残す。
   - 1回ごとに一時停止し、人が位置を確かめて（必要なら置き直して）から次へ。オドメトリのずれが走行ごとに積み重なるため。
   - WheelUnit の状態バイト（過熱）と電池電圧を毎回記録し、閾値を超えたら中止。
9. **PC と機体のつなぎ方を決める**（8章の1）。

## 8. ユーザーと決めること（→ 2026-09-24 に決定。結果は 8.1）
1. **どこで最適化を回すか**:
   - (a) MainBoard だけ: 決めておいた設定の組（数個）を順に走らせ、区間ごとの要約（時間・最高速度・行き過ぎ・止まった位置・滑りの割合）を RAM に残す。PC が走行後にまとめて読み、次の組を決める。**通信の拡張が要らず、始めやすい。推奨の第一歩。**
   - (b) Rock5A で回す: Rock5A は Linux で Python が使え、Wi-Fi もある。車輪の角速度と IMU を既に受け取っているので、Rock5A 側でオドメトリを出し、テストの経路も Rock5A から普通の速度指令で出せる。**試合の経路の確認を兼ねられる。** ただし MainBoard のパラメータを書き換えるには SPI の拡張（フレームに空きが無い）か UI シリアルのコマンド追加が要る。
   - (c) PC から SWD で RAM のパラメータを書き換える: ケーブルが要り、ST-Link を挿すと受信が途切れやすい。非推奨。
2. SSL-Vision など、実際の位置を測れる設備が使えるか。
3. 何を「最適」とするか（7章の3の制約と重み）。
4. 1回の調整に使える走行回数と時間、場所の広さ。
5. 向きごとの上限（7章の6）まで入れるか。
6. 先に Rock5A から実際に走らせる確認（`HANDOFF_VOLTAGE_CONTROL.md` 7章の6）を済ませるか。**安全のため、こちらを先に済ませるのを推奨。**

### 8.1 決まったこと（2026-09-24）
| 項目 | 決定 |
|---|---|
| どこで回すか | **MainBoard だけで完結させる**。測る → 決める → 確認する → 保存する、まで機体の上で行う（ユーザーの追加の要望「一度開始を指示したら PC をつながずに STM が判断する」） |
| 開始の操作 | 当面は **ST-Link で RAM に開始の指示を書き**、ケーブルを抜いてから走り出す。将来は Rock5A から指示 |
| 実際の位置の測定 | 当面は機体のセンサのみ（オドメトリ・IMU）。将来は SSL-Vision |
| 何を最適とするか | **制約付きの最速**。停止位置のずれ ≤ 5cm、向きの行き過ぎ ≤ 5°、安全停止なし、スリップ ≤ 2.8V のときの値 |
| 走行の回数・時間 | **会場を想定し、約5回・10分**。長い版は後で作る |
| 向きごとの上限 | 測った結果を記録し、差があれば後で入れる（最初は全方向共通の値を決める） |
| Rock5A からの走行確認 | 後回し |
| 調整する値 | まずトルク上限・S字の加速度上限・S字の角加速度上限の3つ。構造体には 2.1節の値をすべて移す（他の値は必要性を見ながら後で） |
| 走行と走行の間 | 原点に戻って止まり、LED を点滅させながら約15秒待つ（人が置き直してよい）。その後、位置と向きの推定を0に戻して自動で再開 |
| 決まった値の扱い | 確認の走行で合格したときだけ、IMU の較正値と一緒にフラッシュへ保存し、試合でも使う。不合格・中止なら前の値のまま |
| 結果の表示 | 将来は Rock5A 経由。当面は開発時の確認用に RAM に記録を残すだけ（ST-Link で読める） |

## 9. 最初の一歩（案）→ 8.1 の決定を受けて 10章の段階に置き換えた
1. 8章をユーザーと決める。
2. `parammeter.h` の調整対象の値を、`#define` から**実行時に書き換えられる構造体**（例: `VoltTuneParams`、既定値は今の値）に移す。ビルドは変わっても挙動は同じであることを床で1回確かめる。
3. 区間ごとの要約を計算して RAM に残す仕組みを作る（全サンプルの記録は RAM を約 62KB 使うので、複数回分は持てない。要約なら数回分入る）。
4. 7章の1の「測る走行」（向きごとの滑り始め、実際の加減速度）を作り、1〜2回走らせて、探索が要る値を絞る。
5. 残った値を、少ない回数の探索で決める。

## 10. 設計（機体の上で完結させる形）
### 10.1 全体の流れ（完成形、`src/control/auto_tune.c`）
```
IDLE ─(ST-Link で start_seq を書く)→ 10秒待つ（抜いて離れる）
 → 測る走行 → 15秒待つ → 確認1 ─合格→ フラッシュへ保存 → 終了（合格）
                         └不合格→ 0.2V 下げる → 15秒待つ → 確認2 … 最大3回 → 終了（不合格、前の値に戻す）
いつでも中止: テストの安全停止 / WheelUnit の過熱・電源異常ビット / 電池電圧の低下 / Rock5A の指令・緊急停止
             → 出力を止め、前の値に戻す
```
- **測る走行**（約1.5分）: 前・左・斜め2方向・その場旋回の5つの向きについて、トルク上限を 2.0→3.2V まで 0.2V 刻みで上げる。各段で原点から1.0mを往復する（旋回は ±π）。
  - このとき S字の加速度上限は高めにし、トルク上限が効く状態にする。
  - 各段の加速区間で、IMU の実際の加速度と新しいスリップ指標 S を測る。
  - 決め方: トルク上限 = 並進のすべての向きで S ≤ S_ref を満たす最大の段。`max_accel` = その段で実際に出た加速度（向きの中で最小）。`max_ang_accel` は旋回で同じように決める。
  - 向きごとの結果は RAM に残し、8章の5の判断に使う。
- **確認の走行**（約20秒）: 短くした動作パターンを、決めた値で1周走る。制約（8.1）を機体の上で判定する。テストの外側ループの想定減速度は固定の式（例: 0.9×`max_accel`）で決め、探索はしない（7章の2）。
- **S_ref（合格の基準）**: 練習の床で一度だけ、開発時に決める。2.4 / 2.8 / 3.2V で走らせ、「2.8V は合格、3.2V は不合格」になる値にする。PC で RAM を読んで決め、定数として焼き込む。**会場では PC は要らない。**
- **フラッシュ**: 保存先はセクタ7（`CommonLib-C/flash/flash.h`、**書くたびに丸ごと消える**）で、IMU の較正値（`src/unit/imu.c` の `ImuCalibData`、先頭に置かれている）と共用。
  - 先頭の IMU 部分の並びは変えない（今保存されている較正値もそのまま読める）。
  - 調整結果は、magic・版・チェックサム付きで、先頭から 0x100 の位置に置く。
  - どちらかを保存するときは、もう片方を読み出して一緒に書き直す（`Imu_SaveCalibration` も直す）。
  - 保存は停車中だけ行う（1〜2秒止まるため）。

### 10.2 段階（各段の最後に、書き込んで床で走らせる）
1. **土台** → **実装済み・床で確認済み（2026-09-24）**。10.3、10.5。
2. 区間ごとの要約と、新しいスリップ指標を RAM に残す。練習の床で 2.4 / 2.8 / 3.2V を走らせ、S_ref を決める。→ **要約は実装済み（10.6、床で未確認）。S_ref を決める3段階の走行は、ランプ試験（10.7）に置き換えた。**
3. 測る走行と、値の決め方を作る。
4. 確認の走行、下げてやり直す処理、中止の条件、フラッシュへの保存、全体の流れを組み立てる。会場と同じ手順で通しで試す。

### 10.3 段階1で入れたもの
- **`src/config/volt_tune.h/.c`**: 調整できる値の構造体 `VoltTuneParams volt_tune`。
  - 中身は 2.1節の12個（トルク上限、S字の加速度・角加速度・ジャーク、FF係数、PI、積分の上限）と magic・版。
  - 既定値は `parammeter.h` の `#define`（今は「既定値」の意味）。
  - `VoltTune_Sanitize` で安全範囲（トルク上限 0.5〜3.2V、`max_accel` ≤ 8、`max_ang_accel` ≤ 50 など）に収める。magic・版が合わないとき、NaN を含むときは既定値に戻す。起動時と、テストの開始時に呼ぶ。
- **`omni_drive.c`**: 電圧制御の分岐と `OmniDrive_SetControlMode` が `volt_tune` を読む。
  - 電圧制御のときは、ジャークも `volt_tune` の値を使う。速度モードは今のとおり `TCS_*` を使う。
  - 値は今と同じなので、挙動は変わらないはず。
- **`src/control/auto_tune.h/.c`**: ST-Link から開始を指示する入口。
  - `autotune_ctrl`（magic, start_seq, done_seq, test_id, state, result、すべて uint32）は bss にあり、**起動時は0なので勝手には始まらない**。
  - 指示を受けると、10秒 LED0 を速く点滅させてから、動作パターンのテストを1周走らせる。終わると `done_seq = start_seq` にして、`result` に結果を入れる（1: 最後まで、2: 安全停止、3: Rock5A で取り消し）。
- **`main_mode.c`**: Rock5A 未接続の分岐は `AutoTune_Poll` が false のとき（指示が無いとき）に `LocalController_Stop`。
  - Rock5A の指令か緊急停止を受けると `AutoTune_Cancel` で取り消す。
  - **既定で止まるのは今までと同じ。**
- **`local_controller.c`**: 動作パターンのテストの状態を関数内 static から構造体にまとめ、`LocalController_ResetMotionPattern` を足した。電源を入れ直さずに何度でも走らせられる。main_mode.c から直接呼ぶ使い方（10秒待ち、60秒後に UART で CSV）は今までどおり。
- **PC 用スクリプト**（`tools/`、UTF-8 BOM 付き）:
  - `autotune_start.ps1`: 開始の指示を書く。`-Status` を付けると状態を見るだけ。
  - `read_tcs_log.ps1`: RAM ログを読んで CSV にする（`logs/tcs_ram_日時.csv`）。
  - `stlink_ram.ps1`: 上の2つが使う共通の関数。アドレスは毎回 elf から gdb で調べる。

### 10.4 段階1の確かめ方（床）
1. 書き込み、Rock5A 未接続で起動する → **動かないこと**を確かめる。
2. ST-Link をつなぎ、`powershell -ExecutionPolicy Bypass -File tools\autotune_start.ps1` → ケーブルを抜く → 10秒後に動作パターン（1.5倍）を1周走る。
3. ST-Link をつなぎ、`tools\autotune_start.ps1 -Status` で `result=FINISHED` を確かめ、`tools\read_tcs_log.ps1` で記録を読む。
4. もう一度 `autotune_start.ps1` を実行し、電源を入れ直さずに2回目も走ることを確かめる。
5. `docs/data/motion_pattern_2.8V_pd_final_20260923.csv` と比べる。13区間が約19.2秒で、区間ごとのスリップの割合が同程度なら、挙動は変わっていない。

### 10.5 段階1の床での確認結果（2026-09-24、今の機体）
- 4輪とも実測の車輪速度が動くことを `tools/check_wheels.ps1` で確認した（11.3）。走行の記録でも4輪とも 78〜84rad/s まで回っていた。
- `tools/autotune_start.ps1` で開始 → ST-Link を抜く → 10秒後に動作パターン（1.5倍）を1周、を3回走らせた（`logs/stage1_run1〜3.csv`、`logs/` は git 対象外）。`tools/summarize_motion.ps1` で区間ごとに要約できる。
  | | 合計時間 | スリップ（全体） |
  |---|---|---|
  | 前の機体（2026-09-23、2.8V・PD） | 19.24 秒 | 49% |
  | 今の機体 1回目 | 19.25 秒 | 49% |
  | 今の機体 2回目（再起動後） | 19.45 秒 | 48% |
  | 今の機体 3回目（電源を入れ直さず続けて） | 18.84 秒 | 52% |
- 3回とも `result=FINISHED`。電源を入れ直さずに続けて走らせられる（`LocalController_ResetMotionPattern` が効く）ことも確認した。
- **今の機体でも、現在の値（2.8V など）のまま前の機体と同じ挙動だった**。表・床の負荷分・トルク上限・FF/PI を今すぐ取り直す必要は見つかっていない（オドメトリの時間とスリップの割合での比較。止まった位置・行き過ぎは未評価）。
- 区間ごとのばらつきは、斜めで最大 ±0.5 秒、スリップは ±20 ポイントほど。1回の比較で良し悪しを決めない（2.2）。車輪速度の単発の跳び（123〜205rad/s）が記録に出る。
- 開発中の設定: `AUTOTUNE_IGNORE_ROCK_COMMANDS=1`（`parammeter.h`）。Rock5A の指令を受け付けない。Rock5A の信号が来ていない間は `emergency_stop` が常に1なので、テストの取り消しは「信号を受信していて emergency_stop=1」のときだけにした。**Rock5A での走行確認・試合の前に 0 に戻すこと。**
- 開始の指示を書いたら**すぐ ST-Link を抜く**こと。抜かないまま走らせて機体を押さえると、安全停止するか、再起動して指示が消える。

### 10.6 段階2で入れたもの（2026-09-24、床で未確認）
- **`src/control/motion_summary.h/.c`**: 動作パターンの区間ごとの要約を、1kHz で計算して RAM に残す（`motion_results[8]`、1走行 436byte、直近8回、電源を切ると消える）。
  - 中身: 所要時間、`is_slipping` の割合、出力を縮めた割合、最高速度、**候補のスリップ指標2つ**（A: `s_acc` = 加速中の max(0, |a_odom|−|a_imu|) の平均〔TCS の判定に依存しない〕、B: `gap` = オドメトリ速度と推定対地速度の差〔推定は TCS に依存〕）、進む向きからの外れ、通り過ぎた距離、向きの行き過ぎ、区間の終わりの位置・向きの誤差、WheelUnit の状態バイトの OR、電池電圧の最小。
  - 既存の記録で先に評価した結果: B（オドメトリ − 推定対地速度）は、2.8V で 0.03〜0.04m/s、3.2V で 0.11m/s と区別でき、機体を変えても再現した。A は既存の記録（x 方向のみ）では評価できなかったので、床で同時に取って比べる。
- **`autotune_ctrl` に上書きの欄**（`traction_x100`、`max_accel_x100`、`max_ang_accel_x100`）: 開始の指示と一緒に書くと、その1回だけ `volt_tune` を上書きする（0 は既定値）。走り終わる（取り消す）と既定値に戻る。範囲は `VoltTune_Sanitize` が収める。
- **動作パターンのテストの外側ループ**: 想定減速度を `0.9 × volt_tune.max_accel`、角減速度を `25 × (max_ang_accel / 38)` にした（既定値では従来と同じ 4.5 / 25）。手動調整のときの「トルク上限と比例させる」を、そのまま式にしたもの。
- **PC 用**: `tools/autotune_start.ps1 -TractionV 2.4 -MaxAccel 4.3 -MaxAngAccel 32.6` のように上書きして開始。`tools/read_motion_results.ps1` で要約を読む（`-All`、`-Csv`）。
- **段階2の走行**（2.4V・2.8V・3.2V を、手動調整のときと同じ比率で）:
  | トルク上限 | `-MaxAccel` | `-MaxAngAccel` |
  |---|---|---|
  | 2.4 | 4.3 | 32.6 |
  | 2.8（既定） | （省略） | （省略） |
  | 3.2 | 5.7 | 43.4 |
  評価は、ユーザーの目視（2.4V=許容、3.2V=滑りすぎ、2.8V=確定）と、`read_motion_results.ps1` の指標を突き合わせて、S_ref を決める。

### 10.7 ランプ試験（2026-09-24、床で未確認）— 3段階の走行と S_ref の較正を置き換える
- ユーザーの提案「電圧を3段階に分けず、なめらかに変えながらギリギリを判定したい」を受けて作った。**限界ギリギリの最高の加速度を狙う**（ユーザー）。余裕の比率は掛けない。試合の動きでギリギリが問題になるかは後で判断する。
- **考え方**: 超える分（center を超える電圧）はモータのトルクに当たる。そのトルク上限を、止まった状態から約0.2秒で 1.5V→3.6V まで上げて加速する（加速度の目標は十分高くして、上限＝実際のトルクにする）。
  - IMU の加速度が一番大きくなったときのトルクを**ピーク（＝限界として採用）**、車輪と IMU の加速度の差（進む向き）が 1.5m/s² を 20ms 超えた所を**滑り始め**として記録する。トルク上限にも IMU と同じ 15Hz の LPF を掛けて、位相をそろえて比べる。
  - 位置（オドメトリ）は判定に使わない（改善案4）。
  - 滑り始めを見つけたら 30ms ピークを拾ってから、トルクを7割に下げてブレーキへ。ブレーキも7割からランプで上げて、減速側を同じ方法で測る。
  - **止まった状態から短時間で上げる理由**: 速度が出ると center が増えて、電圧上限 4.9V までの余裕が減る（前進で約0.8m/s を超えると 3.6V をかけられない）。余裕がランプの値より小さくなったら「電圧の余裕なし」で打ち切る。
- **向きと回数**: 前・後・左・右・斜め4・その場旋回2 の10本。2本ずつ組（1本目は空きのある方へ、2本目は戻る向き）で、組ごとに低速で原点・向き0へ戻る。2回くり返し、ピークの差が 0.2V 以上か 10% 以上の向きの組だけ3回目（ユーザー案）。3回でも差が大きい向きは「不安定」。1本は約0.3〜0.5m、全体で約30〜40秒の見込み。
- **採用する値**（`tools/read_ramp_results.ps1` が計算して表示。機体の上での決定・保存は段階3以降）: トルク上限 = 並進8向きのピークの最小（くり返しの最小、3.2V 以下に収める）、加速度上限 = 並進8向きの IMU の加速度の最小、角加速度上限 = 旋回2向きの最小。
- 3.2V を超えるのはこの試験の中の一瞬だけ（ユーザー了承、上限 3.6V）。`VoltTune_Sanitize` は通さず、試験の定数で上限を持つ。
- **ファイル**: `src/control/ramp_test.h/.c`（状態機械、結果は RAM の `ramp_result`）、`auto_tune.c`（`test_id=2`）、`tools/read_ramp_results.ps1`。開始は `tools\autotune_start.ps1 -TestId 2`。波形は `tools\read_tcs_log.ps1`（`tcs_on` 列=何本目か、`target_vx` 列=フィルタ後のトルク上限 [mV]、10ms 周期、加速・ブレーキの間だけ）。
- **気づいたこと（未対応）**: `omni_drive.c` の電圧上限の扱い `allowed = 4.9 − |center|` は、ブレーキ（超える分が負）にも同じ余裕を使っている。実際にはブレーキ側は `4.9 + |center|` まで使えるので、速度が出ているときのブレーキのトルクを必要以上に絞っている可能性がある。ランプ試験のブレーキの結果（「電圧の余裕」で頭打ちになるか）を見てから判断する。

### 10.8 ランプ試験の初回の床走行（2026-09-24、今の機体、`docs/data/ramp_test_20260924_run1*.csv`）
- 動作: 30本（10向き × 3回。差が大きく全向きで3回目に入った）を最後まで走った（`result=FINISHED`、安全停止なし、WheelUnit の状態バイトは全本 1、電池 23V）。同じファームで動作パターンも1回走らせ、区間ごとの要約（`motion_results`）が 30ms 周期のログの要約と一致することを確認した（所要時間 ±0.02 秒、最高速度 ±0.03m/s）。
- **使える結果**: 車輪と IMU の加速度が離れ始める「**滑り始め**」は、向きごとに再現した。
  | 向き | 加速の滑り始め〔V〕（各回） | 中央値 |
  |---|---|---|
  | 前 +x | 2.11 / 2.14 / 2.28 | 2.14 |
  | 後 −x | 2.14 / 2.45 / 2.66 | 2.45 |
  | 左 +y | 2.12 / 2.16 / 2.27 | 2.16 |
  | 右 −y | 2.11 / 2.37 / 2.39 | 2.37 |
  | 左前 | 2.41 / 2.74 / 3.24 | 2.74 |
  | 右後 | 2.33 / 2.43 / 2.76 | 2.43 |
  | 右前 | 2.66（他は電圧の余裕なし） | 2.66 |
  | 左後 | 2.74 / 3.10（他は電圧の余裕なし） | 2.92 |
  - **前後左右は 2.1〜2.5V、斜めは 2.4〜3.2V** で滑り始める。5.10 の見立て「前後左右は斜めより先に滑る」が、機体の上で測って再現した。
  - 手動で決めた 2.8V は、前後左右では滑り始めの 0.4〜0.7V 上（＝はっきり空転して使っている）にあたる。「手動調整のとき前後左右のスリップ 58〜69%」と合う。
  - 減速（ブレーキ）の滑り始めは 1.7〜2.6V。加速より低い（ロックしやすい）。
- **使えない結果（採用してはいけない）**: `read_ramp_results.ps1` が出した「トルク上限 1.84V、加速度上限 2.74 m/s²、角加速度上限 141 rad/s²」は**採用しない**。理由:
  1. **ピークの定義が壊れている**: 滑り始めを見つけたらランプを止める設計のため、「IMU の加速度が一番大きいトルク」は「滑り始め＋約0.3V」に切り詰められ、本当のピークを測っていない。波形では、滑り始めのあとも IMU の加速度は伸びていた（前 +x: 3.1→4.6m/s²）。
  2. **ランプが速すぎてノイズが大きい**: 1.5→3.6V を約0.2秒で上げると、IMU の加速度（15Hz LPF＋機体の振動）が追いつかず、同じ向きの同じ試験でピークが 1.84〜2.56V とばらつく（10向き中9向きで「差が大きい」と判定された。0.2V/10% の基準が厳しすぎる）。
  3. **旋回は滑らなかった**: 12rad/s の速度上限か電圧の余裕に先に達して、滑り始めが見つからない（6本すべて）。角加速度の限界は測れていない（IMU の加速度は 141〜151rad/s² と、手動の 38 よりずっと大きい。手動の 38 は滑りではなく行き過ぎで決めた値）。
  4. 斜め（右前・左後）は、電圧の余裕（4.9V−今の回転数の電圧）が先に尽きて、滑り始めまで測れない本が多い。
- 波形のログは 10ms 周期で 1000 サンプル（27本目まで）しか入らない。
- **次に直すこと（提案、ユーザー確認待ち）**: ランプを止めずに、IMU の加速度が落ち始める（または上限）まで上げ続ける／ランプをゆっくりにする（0.5秒程度）／3回目を測る基準をゆるめる（例: 0.3V か 15%）／旋回は速度の上限を上げるか、滑らないことを「上限以上」として記録する。滑り始め（車輪と IMU の離れ）を主指標にして、ピークは補助にする。

### 10.9 ランプ試験の見直しと2回目・3回目の走行（2026-09-24、`docs/data/ramp_test_20260924_run2.csv`、`run3*.csv`）
- **見直し**（ユーザーの指摘「ゆっくりにすると電圧が足りなくなるのでは」を受けて）: 上限 3.6V→**3.2V**、始め 1.5→**1.8V**、上げる速さ 10→**5V/s**（3.2V まで約0.28秒）、3回目を測る基準を滑り始めの差 **0.3V / 15%**（初回は 0.2V/10% で10向き中9向きが3回目に入った）、旋回は滑らなくても「上限以上」として記録して採用しない。
- **run2（22本）**: 3回目に入ったのは左右（±y）だけに減った。ただし、`ramp_test.c` の不具合で、滑り始めのあとはランプが止まっていた（`ramping=false`）ため、ピークは「滑り始め」に切り詰められていた。→ run2 のピークは無効。
- **run3（24本、不具合を直したあと）**: 滑り始めのあとも、滑り始め+0.4V（または IMU の加速度がピークの8割を下回るまで）上げ続けた。
  - 波形（前 +x・後 −x）: 機体の加速度（IMU）は **約 2.0V で 3〜3.7 m/s² に達したあと、ほぼ頭打ち**（+0.5V で 1〜2割伸びる程度）。車輪の加速度（オドメトリ）だけが 4→13 m/s² に増える（空転）。IMU の加速度は、+0.4V の範囲では落ちなかった。
  - 滑り始め〔V〕（run3、各回）: 前 2.08/2.20、後 2.31/2.23、左 2.11/2.19/2.51、右 2.60/2.23/2.73、左前 2.44/2.72、右後 2.77/2.25/2.74、右前 2.57、左後 2.52（斜めは電圧の余裕が先に尽きる本がある）。ピークの加速度は 2.7〜6.4 m/s²。
  - ブレーキ（減速）の滑り始めは 1.55〜2.3V で、加速より低い。
  - 旋回は滑らない（速度の上限 12rad/s に先に達する）。角加速度は約 150rad/s²（手動の 38 は行き過ぎで決めた値で、滑りの限界ではない）。
- **run1〜3 を通して分かったこと**:
  - 滑り始めの誤差は、同じ向きの走行間で ±0.3〜0.5V。左右（±y）が特にばらつく（左 2.1〜2.7V、右 1.9〜2.7V。右は加速度も左より2割低い傾向）。前後は安定（2.1〜2.3V）。
  - この床で出せる機体の加速度は、前後で約 3.5〜4.5 m/s²、左右で約 2.7〜3.7 m/s²（手動の加速度上限 5.0 は、実際には出ていない）。
  - **1回走らせるだけで自動計算した値は、そのまま採用できない**（測ったトルク上限は約 2.1V、加速度上限は約 3 m/s² で、手動の 2.8V / 5.0 m/s² とかなり違う）。滑り始めより上げても、機体の加速度が少し伸びる（手動調整ではそれで区間の時間が縮んだ）可能性があるため、**採用する前に、その値で動作パターンを走らせて、手動の値と時間・スリップ・目視を比べる必要がある**（次のステップ）。
- **ST-Link を抜いたとき、MainBoard が再起動して指示が消えたことが1回あった**（run3 の1回目。RCC_CSR はウォッチドッグのリセットではなかった。原因不明。2回目は SWD コネクタを先に抜いて問題なし）。開始の指示を書いたら、`autotune_start.ps1 -Status` で `state=WAITING` を確かめてから抜くこと。

### 10.10 速度別の測定を追加（2026-09-24、実装済み・床で未確認）
- **理由**: ランプ試験（10.7〜10.9）は、止まった状態から 0.04〜0.24m の短い1本で、最高 1.2m/s までしか測れていない。手動調整の動作パターン（1.5〜3m、最高 3m/s）と比べるには、高速域が足りない（ユーザーの指摘）。
- **使い方**: `tools\autotune_start.ps1 -TestId 2 -Speeds 0,1,1.5,2`（`0`: 従来の止まった状態から、`1`: 前後左右 1.0m/s、`1.5`: 斜め 1.5m/s、`2`: 前後左右 2.0m/s、`2d`: 斜め 2.0m/s、`3`: 前後左右 3.0m/s）。省略すると `0` だけ（従来どおり）。読み出しは `tools\read_ramp_results.ps1`（低速の表、速度別の表、**ブレーキの停止距離表**、加速側の限界の速度依存）。
- **走らせ方**（速度別の1本）: 始点へ低速で移動（0.8m/s）→ 助走（v0 まで、トルク上限 2.2V・加速度 3.0m/s² の滑らない範囲で巡航し、速度が v0 で加速度がほぼ 0 を 50ms 保つ）→ ランプ加速（目標速度は v0+2、打ち切りは v0+0.5m/s、または上限 3.2V、または電圧の余裕）→ ブレーキ（低速と同じ、弱い所から上げていく）→ 停止 → 次の本。速度の低い順に進む。
- **テスト範囲**（ユーザー決定、選択肢 C）: **前 3.5m・後ろ 1.0m・左右 2.5m**（原点はスタート位置）。安全停止の範囲は x: −1.0〜3.5m、y: ±2.5m（速度別の段を指定したときだけ。低速だけのときは従来の狭い範囲）。動作パターンのテストの範囲は変えていない。
- **範囲の予測**: 各本の前に、必要な長さ（助走＋ランプ＋ブレーキ）を予測し、範囲の端から 0.3m 内側に収まる経路（範囲の中心を経路の真ん中にする。+向きと −向きの組は同じ経路を往復する）だけを走らせる。同じ向きで低い速度の測定が済んでいれば、その距離を伸ばして予測する（助走とブレーキは速度の2乗、ランプは (v0+0.2) に比例）。**収まらない本は走らせずに「範囲不足」と記録する**（3.0m/s の前後は、最悪の場合の余裕が 0.15m と薄いので、2.0m/s の結果しだいで飛ばされる）。
- **記録**: `RampStrokeResult` を 40byte に拡張（助走の速度 v0、ブレーキ開始の速度、停止距離、加速距離、助走距離）。`RampRunResult` は 2260byte（最大56本）。波形のログ（10ms→速度別は 20ms 周期）は、速度別の測定を始めるときにリセットされる（低速のぶんは結果の表に残る）。
- **順序（推奨）**: まず `-Speeds 0,1,1.5,2`（斜め 1.5m/s を含む）→ 停止距離を見る → `-Speeds 3` を、左右から（実際は前後左右を一緒に走らせるが、予測が範囲不足と出た本は飛ばされる）。
- **初回の床走行（速度別、`docs/data/ramp_speed_20260924_run1_aborted.csv`）**: 低速の26本まで走り、速度別の最初の本（前 1.0m/s）のあと、障害物に当たって安全停止（28本目が巡航の時間切れ）。**そのため速度別の停止距離表は取れていない**（27本目のブレーキは 1.53m/s から 0.106m、平均減速度 11m/s² と出ているが、障害物に当たった記録で無効）。低速の結果は、これまでと同じ傾向（滑り始め: 前 1.9、後 2.1〜2.3、左 2.1〜2.3、右 2.1〜2.6、斜め 2.0〜2.9V）。
- **27本目から見つけた不具合を直した**: (1) 助走の距離が 0.88m と出ていた（1.0m/s なら約 0.2m）: 速度が v0 に着いたあとも、加速度が 0.7m/s² 未満という条件が満たされず巡航が続き、その分まで数えていた → 助走の距離は v0 に**最初に着くまで**とし、着いたと見なす加速度を 1.2m/s² にゆるめ、距離の予測に巡航の余裕（v0×0.3秒）を足した。(2) ランプ加速が、車輪の速度（オドメトリ）で打ち切られていた（空転すると実際より速く見えるため、1.0→1.5m/s を約0.06秒、加速距離 0.08m で終わった）→ 打ち切りの速度、ブレーキの開始速度、加速・ブレーキの距離を、**IMU の加速度の積分（機体の実際の速度）**で測る。ブレーキが終わるのは、車輪の速度が止まり、かつ機体の速度も 0.4m/s 未満になったとき。ランプ加速の最長は 0.6 秒。
- **速度別の2回目の床走行（`docs/data/ramp_speed_20260924_run2_aborted.csv`）**: 前後左右 1.0m/s の4本を走ったあと、5本目（斜め 1.5m/s）で障害物に当たって安全停止（巡航の時間切れ）。**4本のデータに、判定の誤りが見つかった**: (1) **加速の滑り始めが4本とも 1.9V**（＝ランプの開始 1.8V の直後）: 助走（トルクの差がほぼ 0）から急に 1.8V を掛けると、車輪だけ先に加速する過渡が出て、滑り始めと誤判定していた。(2) **ブレーキの滑り始めが4本とも 1.42V**（＝ブレーキのランプの開始点 0.7×1.9V の直後）で、同じ過渡の誤判定。そのあと、トルクを「見つけた値の7割」に下げるので、ブレーキが弱いまま止まった（1.1〜1.2m/s から 0.33〜0.49m、平均減速度 1.5〜1.8m/s²）。**低速の試験（v0=0）のブレーキの滑り始め（1.4〜2.3V、開始点の近く）も、同じ誤判定を含んでいる可能性がある。「ブレーキは加速より滑りやすい」という 10.8〜10.9 の結論は保留。**
- **直した**: (1) 速度別の加速のランプは 1.0V から始め、1.8V までは 10V/s、そのあとは 5V/s。判定は開始から 0.10 秒経ってから。(2) ブレーキのランプは 0.8V から始め、1.4V までは 10V/s、そのあとは 5V/s。判定は開始から 0.10 秒経ってから。滑り始め（ロック）を見つけたら、トルク上限を見つけた値の 0.9 倍（前は 0.7 倍）に下げる（ロックを止めて、限界の近くで止める。見つからなければ上限 3.2V まで上げる）。(3) **範囲を走行ごとに指定できる**: `autotune_start.ps1 -XMin -0.5 -XMax 2.0 -YAbs 1.5`（原点=スタート位置、[m]）。省略すると前 3.5・後ろ 1.0・左右 2.5。範囲に収まらない本は、走らせずに「範囲不足」と記録して飛ばす（範囲が狭いと 2.0m/s は飛ばされる。2.0m/s の1本は、予測でおよそ 3.0m の長さと、両端の余裕 0.6m で、合計 3.6m の直線が要る）。
- **速度別の3回目の床走行（エリア 3.5×2.5m、`-Speeds 1,1.5 -Rect 3.5,2.5 -Rotate`、`docs/data/ramp_speed_20260924_run3_aborted.csv`）**: 前後 1.0m/s の2本は範囲内で完了。3本目（左 1.0m/s）の助走の相で、ランプを始める条件（速度が v0±0.05m/s で IMU の加速度 1.2m/s² 未満を 50ms 保つ）が満たされず、v0 に着いたあとも約 1.9 秒巡航し続けて、y が 1.051m（範囲 ±1.05m）に達して安全停止（範囲外）。ぶつかってはいない。左右の助走は、v0 に着くまで 0.54m かかった（前後は 0.13〜0.15m）。2回目（90° 回して繰り返す）は未確認。
  - 前後 1.0m/s のブレーキ: 開始 1.2〜1.4m/s から 0.34〜0.41m で止まった（平均減速度 2.3m/s²、ロックは見つからず「滑らず停止」）。1.0m/s のブレーキは短く、0.8V から 5V/s では限界まで上げる前に止まるため、これは限界の減速度ではない。
- **直した**: 助走は、v0 に着いてから 0.25 秒経ったら（またはゆるめた条件を保ったら）ランプを始める。v0 に着かないまま予測した長さの 55% を進んだら、その1本はやめて止まる（範囲を出ないように）。助走のトルク上限を 2.2→2.0V、加速度上限を 3.0→2.5m/s² にした（左右の滑り始めの近くまで上げない）。ブレーキのランプは 1.0V から始め、2.0V までは 12V/s、そのあと 6V/s（判定は開始から 0.06 秒後）。
- **速度別の4回目の床走行（成功、`docs/data/ramp_speed_20260924_run4_rotate.csv`）**: `-Speeds 1,1.5 -Rect 3.5,2.5 -Rotate`。**16本を最後まで完走**（1回目 8本、機体が右へ 90° 回った2回目 8本。安全停止なし、ぶつかってもいない）。エリアのど真ん中に置いて、機体が自分で 90° 回る動きも問題なし。
  - **加速側**: 滑り始めは v0=1.0m/s で、前 1.83〜2.09V、左 1.90〜2.01V、右 2.10〜2.22V（低速 v0=0 の結果 1.9〜2.6V と同じ範囲＝滑り始めは 1.5m/s くらいまで速度にあまり依存しない）。後 (−x) は滑る前に電圧の余裕で頭打ち。斜め 1.5m/s は、4本中3〜4本が滑り始めの前に「電圧の余裕なし」（トルク上限 約 2.0V 付近）で頭打ち＝**速度が上がると電圧の余裕が先に効く**（試算と合う）。1回目と2回目（機体の向きが 90° 違う）の滑り始めの差は 0.1〜0.25V で、床の場所による差は大きくない。
  - **IMU の加速度ピーク**: 加速 平均 4.3m/s²（v0=1: 4.0、1.5: 4.7）、減速 平均 5.3m/s²（v0=1: 5.1、1.5: 5.5）。**16本中13本で減速のピークが加速のピークより大きい（平均 +22%）**。「減速のほうがやりやすい」という報告と一致する（ただしピークは短いスパイクで、LPF 済みの IMU の値）。
  - **ブレーキの停止距離**（ロック判定の誤りは残る）: 1.0m/s の設定（開始 1.0〜1.3m/s）で 0.26〜0.40m、1.5m/s の斜め（開始 1.5〜1.7m/s）で 0.46〜0.74m。停止距離から出した平均減速度は約 2.3m/s²（IMU のピーク 5m/s² より低いのは、ブレーキのランプが 1.0V から 0.28 秒かけて上がる分と、止まる間際の弱い部分を含むため。上限側の値）。**ブレーキの滑り始め（ロック）は 1.58V に集中しており、判定の開始直後に出ている＝まだ過渡の誤判定が残っている**（ブレーキ側の滑り始めは信頼できない）。
  - **2.0m/s の走らせ方（この試験の限界）**: 使えるエリア（3.5×2.5m、範囲は内側 0.2m、経路は端から 0.3m 内側）だと、長辺の直線は 2.5m。2.0m/s の1本は、同じ run の 1.0m/s の測定から伸ばすと約 3.0m と予測されて**範囲不足で飛ばされる**（助走 0.7＋巡航 0.3＋ランプ 0.3＋ブレーキ 1.1〜1.3）。3.0m/s は、ブレーキだけでも約 3.5m 要るので、このエリアでは走れない。**高い速度は「ブレーキのみ」（助走してブレーキだけ）にすれば 2.0〜2.5m/s まで入る見込み。**
- **エリア 3.5×2.5m の長方形での使い方（ユーザーの環境）と、2回目の自動化**: `autotune_start.ps1 -TestId 2 -Speeds 1,1.5 -Rect 3.5,2.5 -Rotate`。
  - 置き方: 機体の中心を**エリアのど真ん中（対角線の交点）**に、**前を長辺に沿って**向けて置く（ユーザーの希望で、切りのいい位置にした）。`-Rect 3.5,2.5` は範囲を x: ±1.55m、y: ±1.05m（各端から 0.2m 内側）にする。2回目は、その場で右へ 90° 回るだけ（移動は要らない）。
  - `-Rotate`（`RAMP_SPEED_ROTATE`、mask の 0x80）: 1回目（機体の前が長辺に沿う）が終わると、機体が**自分で範囲の真ん中（原点）へ戻り、右へ 90° 回って**、範囲の縦横を入れ替えて（x: ±1.05m、y: ±1.55m）もう一度繰り返す（2回目）。機体の左（+y）が長辺に沿うので、左右の 2.0m/s は2回目で走る。低速（v0=0）は繰り返さない。結果の「回」の欄は、速度別の 1回目=1、2回目=2。
  - 2.0m/s は、範囲に収まると予測した本だけ走る（直線に約 2.5m 要る。1回目の前後、2回目の左右が収まる見込み）。最初は `-Speeds 1,1.5` で判定とブレーキを確かめ、そのあと `-Speeds 1,1.5,2`。
- **加速と減速のどちらが強いか**: 「減速のほうがやりやすい」という報告はある（ブレーキは逆向きの電圧が使えて余裕が増える、加速は高速で逆起電力で頭打ちになる。今の実装は逆にブレーキを絞っている）。**この機体の実測は、ブレーキ側の判定の誤り（上）が直るまで信頼できない。** 直した版の停止距離表（ブレーキを上限 3.2V まで上げて測る）で確かめる。
- **速度別の4回目の床走行（エリア 3.5×2.5m、`-Speeds 1,1.5 -Rect 3.5,2.5 -Rotate`、`docs/data/ramp_speed_20260924_run4.csv`）**: 16本（1.0m/s の前後左右、1.5m/s の斜め、各2回。2回目は機体が自分で右へ 90° 回って続けた）を完走。安全停止なし。
  - 加速側の滑り始めは、1.0m/s で前 1.8〜2.1、左 1.9〜2.0、右 2.1〜2.2V（後は判定できず）。止まった状態からの結果（1.9〜2.6V）と同じ範囲で、**1.5m/s くらいまでは速度にあまり依存しない**。1回目と2回目（機体の向きが 90° 違う）の差は 0.1〜0.25V。
  - **斜め 1.5m/s は、滑り始めの前に電圧の余裕で頭打ち**（8本中6本が「電圧の余裕なし」、トルク上限が約 2.0V の付近）。試算（速度が出ると加速側の余裕が減る）と合う。
  - **IMU の加速度ピーク: 加速の平均 4.3m/s²、減速の平均 5.3m/s²**（16本中13本で減速のほうが大きく、平均で約 22% 上）。ブレーキのほうが強い傾向（電圧の余裕が、ブレーキ側は逆向きの電圧が使えて増える）。ただしブレーキの滑り始めは 1.58V に集中しており、判定の直後に出る誤判定の疑いが残る（下記の修正）。
  - ブレーキの停止距離（弱い所から上げていくので上限側の値）: 1.0m/s の前後左右で 0.26〜0.40m、斜め 1.5m/s で 0.46〜0.74m。平均の減速度は約 2.3m/s²（IMU のピーク約 5m/s² より低い）。
- **10.10 の追加（実装済み・床で未確認）**: (1) **ブレーキの判定の開始を 0.06→0.10 秒にした**（過渡の誤判定を、さらに避ける）。(2) **ブレーキのみの試験を追加**（`-Speeds b2`: 前後左右 2.0m/s、`b2.5`: 2.5m/s）: v0 まで助走してから、加速せずに直接ブレーキ。1.0V から 12V/s（2.0V まで）→ 6V/s で上げ、**IMU の減速度がピークを過ぎて 20% 落ち始めたら（30ms 続いたら）、そのトルクで保つ**（ロックで滑らせない）。停止距離と、IMU の減速度ピークを記録する。結果の表では、加速の終わり方が「-」で「ブレーキのみ」と出る。エリア 3.5×2.5m では、2.0m/s の前後（1回目）と左右（90° 回した2回目）が走る見込み。2.5m/s は予測が範囲不足で飛ばされやすい。(3) **斜めのランプを限界まで上げる**（ユーザー決定「物理面・安全面等から問題がなければ限界まで」）: ランプの上限を 3.2→**4.5V**、斜めのランプの速さを 5→**8V/s** にした。物理・安全面の確認: 出力の電圧は `omni_drive.c` で `min(4.9V − 回転数分の電圧, トルク上限)` に収まるので、上限を上げても WheelUnit への電圧は 4.9V を超えない（WheelUnit の保護は +5.0V が1秒続いたとき。ランプは1本1秒未満）。過熱・電源異常は状態バイトで見て止める。滑り始めを見つけたら加速は約 +0.4V で止まる。採用する値の安全範囲（`volt_tune.c` の 3.2V）は変えない。**斜めが 3.2V に届かなかった主因は、上限ではなく電圧の余裕（4.9V − 回転数分）だった**ので、上限の引き上げだけでなく、速度が出る前に上げ切るようランプを速くした。
- **5回目の床走行（`-Speeds 0,1,1.5,b2 -Rect 3.5,2.5 -Rotate`、`docs/data/ramp_speed_20260924_run5*.csv`）**: 46本を最後まで走った（低速の10向き＋3回目 22本、速度別 24本）。安全停止なし。電池は 23〜24V（記録の電池 V 欄）、25V（読み出し）。**「電池電圧 0V」と出ていたのは、私が古い固定アドレスを読んでいた誤りだった**（構造体の配置が変わり、`robot.info.battery_voltage` のアドレスがずれた）。実際の電池は入っていた。`check_wheels.ps1` は、電池をシンボルから引いて表示するようにした。
  - **ブレーキのみ 2.0m/s（前後だけ走った）**: 前 2.06m/s から 0.665m、後 1.90m/s から 0.56m で停止（平均の減速度 3.2m/s²）。IMU の減速度ピークは 6.9〜9.1m/s²、ピークのトルクは 2.3〜2.4V、ロックの兆候（車輪の速度が正負に振れる）が 2.4V 付近から出る。1.0〜1.5m/s のブレーキは 0.23〜0.55m（平均の減速度 2.4〜2.8m/s²）。
  - **加速と減速の IMU ピーク**: 15本の平均で、加速 3.7、減速 6.1m/s²。**ただしピーク（15Hz の LPF を掛けた IMU の最大値）は、ロックのばたつきで実際より大きく出る**。波形（前 2.0m/s のブレーキ）では、継続する減速度は 3〜5m/s² 程度（ピークの 6.9 の約 6〜7割）。**加速に比べて減速のほうが強い傾向はあるが、「1.7倍」は過大。同じ条件（区間の平均や、ピーク前後の平均）で比べ直す必要がある。**
  - **左右の助走が弱い**: 1.0m/s まで着く距離が、前後 0.15〜0.19m に対して、左 0.39〜0.65m、右 0.14〜0.39m。助走のトルク上限 2.0V・加速度上限 2.5m/s² では、左右は約 1〜1.5m/s²しか出ない（左右の力配分が前後の 1.3〜1.6倍要るため）。左右 1.0m/s の1本は「時間切れ」（助走が v0 に届かなかった）、2.0m/s の右は同じ理由で時間切れ。**このエリア（3.5×2.5m）では、左右 2.0m/s は走らせられない**（予測 3.1m の直線に対し、使える長さは 2.5m）。
  - **範囲不足で走らなかった本**: ブレーキのみ 2.0m/s の左右（1回目）、前後（2回目）。前後は1回目、左右は2回目で走る想定だったが、左右は助走が長く予測が 3.1m と出て飛ばされた。
  - **ブレーキのみの「保持」が働かない本があった**（後 2.0m/s）: IMU の減速度が20%落ちる状態が、ばたつきで30ms 続かず、トルクが 4.5V まで上がった（速度が落ちたあとなので実害はない）。ピークを過ぎて落ち始めたか、滑り始めから +0.4V で保持するように直す。
  - **斜め（4.5V・8V/s）**: 低速（v0=0）の斜め 8本のうち、滑り始めが見つかったのは 6本（2.24〜3.09V）、残りは電圧の余裕（2.6〜3.3V）で頭打ち。1.5m/s の斜め 8本は、6本が電圧の余裕（約 1.9〜2.3V）で頭打ち。**斜めの限界は摩擦より電圧の余裕で決まる**（トルクを上げても、逆起電力で使える電圧が尽きる）。物理限界なので、ここから先は上げられない。
  - 3回目を測ったのは斜め2向き（左前、右後）だけ。左前は 2.26 / 3.09 / 2.87V と、ばらつきが大きい（不安定）。
- **5回目の結果を受けた修正（実装済み・床で未確認）**: (1) **ブレーキのみの保持を確実に**: 3つの条件のどれかで、ピークのトルクで保つ（IMU の減速度の落ち始め〔20%・30ms〕、滑り始めから +0.4V、ピークのトルクから +0.8V）。(2) **加速度の平均を記録**: 直近 100ms の IMU の加速度の平均（1ms ごとのリング）を、ピークを更新するたびに保存（`RampPhaseResult` に `avg_acc`、14byte。`RampStrokeResult` 44byte、`RampRunResult` 2484byte）。加速と減速の比較を、ピークと平均の両方で表示する（ピークは、ロックのばたつきで実際より大きく出る）。(3) **左右の助走**: トルク上限を左右だけ 2.0→2.3V、距離の予測の既定の加速度を左右は 1.5m/s²（前後・斜めは 2.5m/s²）。(4) **ブレーキのみの向き**: `b2`・`b2.5` は前後だけ、左右は `b2l`（このエリア 3.5×2.5m では走らせられない）。(5) **ブレーキのみの開始を 1.0→1.6V**（上がる間の停止距離の延びを減らす）。(6) 読み出しに、範囲不足・時間切れの内訳と、ブレーキのみの要約を追加。
- **6回目の床走行（修正版、`-Speeds 0,1,1.5,b2 -Rect 3.5,2.5 -Rotate`、`docs/data/ramp_speed_20260924_run6*.csv`）**: 42本を完走（安全停止なし）。ユーザーの目視: 「パッと見問題なさそう。ただ**左右の動きがもたついていたように見えた**」。
  - **ブレーキのみの保持が働いた**: 波形で、トルク上限が 2.9〜3.3V まで上がったあと、ピークのトルク（2.47〜2.49V）まで下がって保ち、そのまま止まった（前回は後 2.0m/s が 4.5V まで上がった）。停止距離は、前 1.88m/s から 0.50m、後 1.96m/s から 0.55m（平均の減速度 **3.5m/s²**、前回は 3.2）。上がる途中（トルク約 3V）で、停止直前に車輪の速度が正負に振れる（トルクが停止に要る分を超えて、車輪が逆転しかける）が、機体は最後まで前へ減速し続けた。
  - **加速度の平均**: ピーク前 100ms の IMU の平均は、加速 1.55、減速 3.21m/s²（14本）。ピーク（加速 4.3、減速 6.3）より、差がはっきり出る（減速が約2倍）。ただし、どちらも 100ms の窓にランプの立ち上がりを含むので、絶対値は低め。**加速より減速のほうが強い傾向は、ピークでも平均でも同じ向き**。
  - **左右が弱い（もたつきの原因の可能性）**: 加速のランプの IMU ピーク（v0≤1m/s、run4〜6 の合計）は、前 4.3、後 5.0、**左 3.1、右 3.1 m/s²**（左右は前後の約 **0.67 倍**）。助走で 1.0m/s に着くまでの距離は、前後 平均 0.17m に対して、左右 平均 0.31m（最大 0.54m）。左右は、同じ加速度に前後の 1.3〜1.6倍の力が要る（力の配分。5.10）うえ、実際に出せる加速度も前後の約 2/3。**手動調整の加速度上限 5.0m/s² は、左右では出せない**。S字の目標が実際より先に行き、PI で追うため、もたつきに見える。向きごとの加速度上限（前後 約 4.3〜5.0、左右 約 3.1）を持たせる根拠になる。
  - **速度別の左右 1.0m/s**: 加速の滑り始めは 1.95〜2.07V（前後 1.83〜2.01V と近い）が、IMU ピークが 3.0〜3.6 で前後（4.3〜5.4）より低い。ブレーキ（v0=1.0）の平均の減速度は、前後 2.50、左右 2.34m/s² とほぼ同じ（左右の減速は、前後と大差ない）。
  - 斜め 1.5m/s の助走が届かず「時間切れ」になった本が2つ（2回目の右前・左後、助走 1.13m まで進んでも 1.5m/s に届かなかった）。2回目の斜めは、機体の向きが 90° 違うので、力の配分が違う可能性がある。
  - 範囲不足: ブレーキのみ 2.0m/s の前後（2回目）は、機体の向きが 90° 違って前後が短辺に沿うため、予測 2.1〜2.2m が使える長さ 1.5m を超えて飛ばされた（想定どおり）。
- **次**: 結果を見て、速度に応じた最大加速度・減速度（電圧の余裕から計算する連続な上限、ブレーキ側の余裕の修正を含む）を入れるか判断する（計画の手順1b）。試算では、ブレーキ側は今 3m/s で 4.9−|center|＝1.5V に絞られていて、実際の余裕は約 7.4V。

### 10.11 左右の FF 係数（`ka_lat`）と FF 試験（2026-09-24、実装済み・床で未確認）
- **目的**: ランプ試験の6回目で、左右が弱い（IMU ピーク 前 4.3、後 5.0、左右 3.1m/s²、左右は前後の 0.67 倍。助走で 1.0m/s に着く距離 前後 0.17m、左右 0.31m）ことが分かり、ユーザーの目視も「左右がもたついた」。**FF が左右の必要な電圧を足りなく見積もっている**（足りない分を遅い PI が補う）という仮説を、実測で較正し、左右の最高加速度・最高速度を詰める（ユーザー方針「2（左右の FF 係数）をベースに」）。
- **`ka_lat`**（`volt_tune`、既定 `WHEEL_VOLT_KA_LAT_BODY`=0.5、今の `ka_lin` と同じなので挙動は変わらない）: `omni_drive.c` の加減速ぶんの FF を、`ka_lin × a[0] × ax + ka_lat × a[1] × ay + ka_ang × a[2] × α` にした（x は前後、y は左右。斜めは和）。`VOLT_TUNE_VERSION` は 2。
- **`volt_tune_base`**（走行ごとの基準 = 既定値＋開始の指示の上書き）を追加し、ランプ試験・FF 試験は、試験の途中で値を変えたあとの復帰に `VoltTune_LoadBase` を使う。`autotune_start.ps1 -KaLat 0.7` で、再書き込みなしに `ka_lat` を試せる（`autotune_ctrl.ka_lat_x1000`）。
- **FF 試験**（`-TestId 2 -Speeds ff -Rect 3.5,2.5 -Rotate`。計画では `test_id=3` としていたが、範囲・回転・安全停止・始点への移動の仕組みを共用するため、ランプ試験の速度の段の1つにした）:
  - 前・後・左・右 × 指令の加速度 1.5 と 2.5 m/s² × 2回。各1本は、始点へ移動 → **PI を切って（`kp_lin=ki_lin=0`）FF だけで**、目標速度 1.5m/s へ S字で加速（トルク上限 3.0V で効かない）→ 指令の加速度が立ち上がって 0.15 秒後〜落ち始めるまでの窓で、IMU の加速度の平均を取る → 通常の設定でブレーキ。予測した長さが範囲に収まらない本は、走らずに記録する（左右は指令の 0.7 倍しか出ない想定で長く見積もる）。
  - 記録は `ff_results`（`FfStepResult` 16byte × 最大48）。`tools/read_ff_results.ps1` が、向きごとの比（IMU / 指令）、直線の傾き・切片、推奨の `ka`（`ka_new = ka × 指令 / IMU`、または傾きから `ka / s`）を表示する。前後は基準（比が 1 に近ければ、FF の考え方は合っていて、左右だけがずれている）。
- **手順**: (1) 動作パターン（既定）を1回走らせ、挙動が変わっていないことを確認（18.8〜19.7秒）。(2) FF 試験を1回 → 推奨 `ka_lat` を得る。(3) `-KaLat <推奨値>` で FF 試験をもう一度 → 左右の比が 1 に近づくか。(4) 安定したら `parammeter.h` の `WHEEL_VOLT_KA_LAT_BODY` に書く。(5) ランプ試験（`-Speeds 0,1`）で、左右のピーク・助走の距離が改善したか。(6) 左右だけの 2.0m/s の段（`l2`）で、左右の最高速度を測る。(7) 向きごとの加速度上限（`max_accel_lat`）を設計する。
- 注意: FF が効くのは、摩擦の限界に達するまでの立ち上がり（もたつき）。左右の摩擦の限界そのもの（約 3.1m/s²）は、FF を上げても超えられない。`ka_lat` を上げすぎると、左右の加速で行き過ぎる。

## 11. 2026-09-24 の追記（Rock5A 通信の調査で分かったこと。自動調整の実装を続けるチャット向け）
### 11.1 機体が変わっている（重要）
- **この文書の値は、機体を変更する前の機体で決めたものです。** 機体を変更したため、次は今の機体で取り直す必要があるかもしれません。
  - 表（`wheel_voltage.c` の `kTableOmegaPos/Neg`）と床の負荷分（`kLoadVolt`）、トルク上限 2.8V、`S_ref`（10.1）、`WHEEL_VOLT_KA_*`、`VEL_FB_*`。
  - 「手動調整で確定した 2.8V」は前の機体の結果。今の機体で同じ評価になるかは未確認。
  - 段階2の「S_ref を決める」走行は、今の機体で行うこと。
- 4輪の WheelUnit のファームを書き直した（電圧モード対応版 `422560b8` 以降、ブランチ `FW/WheelDriver_V26.3`）。**書き込みでキャリブレーションの保存値が消えたかは未確認**（4輪とも動いたので問題は出ていない）。

### 11.2 電圧モードの確認結果
- 機体を浮かせて、電圧制御で 4輪が指令どおり回ることをローカルテストで確認した（前進 +300mm/s で符号 `−,−,+,+`、実測 約 ±10〜13rad/s、目標 約8rad/s、無負荷のため速め）。
- Rock5A からの速度指令でも、車輪が回り、機体が動くことを確認した（浮かせた状態）。**床での走行は未確認。**
- Rock5A → MainBoard の通信は正常（`estop=0 sig=1`、有効フレームの間隔は常に 8ms 以下）。Rock5A 側は問題なし。

### 11.3 WheelUnit まわりで実際に踏んだこと
- **WheelUnit の電源（24V）を入れ忘れると、`meas`（実測の車輪速度）が 4輪ともちょうど 0 になる。** 停止中のノイズ（±数十）も出ないので、それで見分けられる。
- **MainBoard の起動後に WheelUnit の電源を入れると、受信が復帰しないことがある。** MainBoard をリセットすると直る（`STM32_Programmer_CLI -c port=SWD -rst`）。文書 `HANDOFF_VOLTAGE_CONTROL.md` の 5.2 と同じ現象。
- **WheelUnit 1輪のファームだけ不良のことがある。** 症状: 通信は生きている（受信フレーム数は増え、状態バイト=1）のに、報告する車輪速度だけが常に 0。書き直して直った。**自動調整を走らせる前に、4輪とも `meas` が動くことを確認すること**（1輪でも 0 だと、オドメトリが狂い、他の輪の電圧が不自然になる）。
- `rx_stall wheel=2`（ID3）は、まだ数秒おきに出る（既知、5.2 の未解決の項目）。回転そのものには支障が出ていない。

### 11.4 作業手順のメモ
- ビルド: PowerShell で `$env:TMP="$env:LOCALAPPDATA\Temp"; $env:TEMP=$env:TMP; make -j8`（Git Bash の `make` は TEMP がなく失敗する）。
- 書き込み: `& "C:\ST\STM32CubeCLT_1.21.0\STM32CubeProgrammer\bin\STM32_Programmer_CLI.exe" -c port=SWD -w build\MainBoard_V26_2.bin 0x08000000 -v -rst`（機体のリセットも行う）。`No debug probe detected` は ST-Link 未接続。
- シリアルログ: `powershell -ExecutionPolicy Bypass -File tools\serial_log.ps1 -Port COM3 -Seconds 30`（USART1、250000bps）。保存先は `logs\serial_日時.csv`。ポートを他のプログラムが開いていると使えない。
- テストのために `main_mode.c` を一時的に書き換えるときは、**必ず元に戻してから書き込む**（戻し忘れると、Rock5A の信号が来ていない間に車輪が勝手に動く）。`git checkout -- src/mode/main_mode.c` で戻せる。
- この作業ツリーは、複数チャットで同時に触ると、ブランチが detached HEAD になる・コミット漏れが出る、などが起きた。作業の最初に `git status` と現在のブランチを確認すること。
